#include "monty.h"
/**
* nop - does nothing at all
* @top_of_stack: double pointer to the top_of_stack
* @current_line_number: line number of the opcode
* Description: useless opcode
* Return: void
*/

void nop(stack_t **top_of_stack, unsigned int current_line_number)
{
	(void)top_of_stack;
	(void)current_line_number;

}